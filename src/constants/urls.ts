const BASE_URL = "https://www.example.org/ogcapi";
const COLLECTIONS_RELATIVE_URL = "/collections";

enum METHOD {
  GET = "get",
  POST = "post",
}

export { BASE_URL, METHOD, COLLECTIONS_RELATIVE_URL };
